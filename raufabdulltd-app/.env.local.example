# Facebook App credentials for OAuth. Provide your own values when running locally.
FB_APP_ID=
FB_APP_SECRET=

# The redirect URI configured in your Facebook App settings. It must match exactly.
FB_REDIRECT_URI=https://www.raufabdulltd.online/api/auth/facebook/callback

# The Graph API version to use (e.g. v21.0). You can change this to the latest supported version.
FB_API_VERSION=v21.0